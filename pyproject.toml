[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "nupstup"
authors = [{name = "Graziella", email = "graziella@nupstup"}]
dynamic = ["version", "description"]
dependencies = ["pandas", "numpy", "scipy", "gurobipy"]
